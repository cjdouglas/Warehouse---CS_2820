package warehouse;

public class Inventory {
	// get inventory
	// restock inventory
	// count down when items leave
	// Need a class Item : has ID num, Price, Size
	// Shelf class needs a boolean contains(object)

}
